title: Git: Setting your username and email
tags: git

Set your git email and username locally to the repo. I always forget this.

```
git config user.name bumblebee
git config user.email buzz@buzz.buzz
```
